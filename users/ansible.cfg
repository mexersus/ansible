[defaults]
inventory = ./inventory
host_key_checking = False
interpreter_python = auto_silent
callback_result_format = yaml
retry_files_enabled = False

[privilege_escalation]
become = True
become_method = sudo
# Prompt once for the sudo password on every run.
become_ask_pass = True

[ssh_connection]
pipelining = True
